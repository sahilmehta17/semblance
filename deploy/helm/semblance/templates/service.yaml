apiVersion: v1
kind: Service
metadata:
  name: {{ include "semblance.fullname" . }}
  labels:
    {{- include "semblance.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "semblance.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
